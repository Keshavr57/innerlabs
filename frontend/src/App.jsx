/**
 * App Component (Root)
 * 
 * Main application entry point.
 * In a larger app, this would include routing, but for this
 * single-page application, we just render the Home page.
 */

import Home from './pages/Home';

function App() {
  return <Home />;
}

export default App;
